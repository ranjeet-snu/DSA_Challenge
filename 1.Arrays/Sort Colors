class Solution {
public:
    void sortColors(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        for (int i=1;i<nums.size();i++)
        {
            cout<<nums[i]<<" ";
        }
        
    }
};
